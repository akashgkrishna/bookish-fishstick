package has.a.relationship.computer.example;

public class Driver {
    public static void main(String[] args) {
        Computer computer = new Computer(); // Create a Computer (which has CPU and Memory)
        computer.startComputer(); // Start the computer using its components
    }
}
